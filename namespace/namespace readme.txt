* a namespace is a declarative region used to localize global identifier name to avoid names collision

* A namespace create a memory in which various programe elements can be placed 

* elements delcared in one namespace are seperate from element declared in other

*element can be variables functions classes and templates